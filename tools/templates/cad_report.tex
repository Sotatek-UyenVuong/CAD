% CAD Report LaTeX Template — xelatex, fontspec only (no xeCJK/ctex needed)
\documentclass[a4paper,11pt]{article}

% ── Fonts (fontspec — supports Japanese via Noto Sans CJK JP) ──────────────
\usepackage{fontspec}
\setmainfont[
  BoldFont   = Noto Sans CJK JP Bold,
  ItalicFont = Noto Sans CJK JP,
]{Noto Sans CJK JP}
\setsansfont{Noto Sans CJK JP}
\setmonofont[Scale=0.88]{DejaVu Sans Mono}

% ── Layout ─────────────────────────────────────────────────────────────────
\usepackage[
  a4paper,
  top=25mm, bottom=22mm,
  left=25mm, right=20mm,
]{geometry}

\usepackage{setspace}
\setstretch{1.3}

% ── Colors ─────────────────────────────────────────────────────────────────
\usepackage[table]{xcolor}
\definecolor{headerbg}{HTML}{0F3460}
\definecolor{header2bg}{HTML}{16213E}
\definecolor{header3bg}{HTML}{1A5376}
\definecolor{accent}{HTML}{E94560}
\definecolor{rowalt}{HTML}{F0F4FF}
\definecolor{border}{HTML}{CCCCDD}
\definecolor{codebg}{HTML}{F8F9FA}
\definecolor{codefg}{HTML}{2D3436}

% ── Section headings ───────────────────────────────────────────────────────
\usepackage{titlesec}

\titleformat{\section}
  {\Large\bfseries\color{headerbg}}
  {\thesection.}{0.8em}{}
  [\vspace{1pt}\color{headerbg}\rule{\linewidth}{1.5pt}]

\titleformat{\subsection}
  {\large\bfseries\color{header2bg}}
  {\thesubsection}{0.8em}{}

\titleformat{\subsubsection}
  {\normalsize\bfseries\color{header3bg}}
  {\thesubsubsection}{0.8em}{}

\titlespacing*{\section}      {0pt}{20pt}{8pt}
\titlespacing*{\subsection}   {0pt}{14pt}{5pt}
\titlespacing*{\subsubsection}{0pt}{10pt}{3pt}

% ── Tables ─────────────────────────────────────────────────────────────────
\usepackage{longtable}
\usepackage{booktabs}
\usepackage{array}

% Alternating row colors & full-width tables via Pandoc
\usepackage{tabu}
\newcommand{\toprulecolor}{\arrayrulecolor{headerbg}\toprule\arrayrulecolor{black}}

% Make Pandoc longtables look good
\setlength{\LTpre}{6pt}
\setlength{\LTpost}{6pt}

% ── Code blocks ────────────────────────────────────────────────────────────
\usepackage{fancyvrb}
\usepackage{listings}

\DefineVerbatimEnvironment{Highlighting}{Verbatim}{
  commandchars=\\\{\},
  fontsize=\small,
  frame=single,
  framesep=4pt,
  rulecolor=\color{border},
}

% ── Links ──────────────────────────────────────────────────────────────────
\usepackage[
  colorlinks=true,
  linkcolor=headerbg,
  urlcolor=accent,
  citecolor=header2bg,
]{hyperref}

% ── Header / Footer ────────────────────────────────────────────────────────
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0.5pt}
\renewcommand{\footrulewidth}{0.3pt}
\renewcommand{\headrule}{{\color{headerbg}\hrule width\headwidth height\headrulewidth}}
\renewcommand{\footrule}{{\color{border}\hrule width\headwidth height\footrulewidth}}

\fancyhead[L]{\small\color{header2bg}
  $if(title)$$title$$else$CAD Report$endif$}
\fancyhead[R]{\small\color{header2bg}
  $if(date)$$date$$endif$}
\fancyfoot[C]{\small\color{gray} \thepage}

% ── Lists ──────────────────────────────────────────────────────────────────
\usepackage{enumitem}
\setlist[itemize]{leftmargin=1.8em, itemsep=1pt, parsep=0pt, topsep=4pt}
\setlist[enumerate]{leftmargin=1.8em, itemsep=1pt, parsep=0pt, topsep=4pt}

% ── Graphics ───────────────────────────────────────────────────────────────
\usepackage{graphicx}
\usepackage{float}
\makeatletter
\def\maxwidth{\ifdim\Gin@nat@width>\linewidth\linewidth\else\Gin@nat@width\fi}
\makeatother
\setkeys{Gin}{width=\maxwidth,keepaspectratio}

% ── Pandoc compatibility ────────────────────────────────────────────────────
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

% Pandoc Shaded environment for code blocks
\usepackage{framed}
\newenvironment{Shaded}
  {\begin{snugshade}\footnotesize}
  {\end{snugshade}}

% ── Title block ────────────────────────────────────────────────────────────
$if(title)$
\title{
  {\color{headerbg}\rule{\linewidth}{2.5pt}}\\[6pt]
  {\LARGE\bfseries\color{headerbg} $title$}%
  $if(subtitle)$\\[4pt]{\large\color{header2bg} $subtitle$}$endif$\\[4pt]
  {\color{headerbg}\rule{\linewidth}{1pt}}\\[-8pt]
  \vspace{0pt}
}
\author{$if(author)$$author$$else$$endif$}
\date{$if(date)$$date$$else$$endif$}
$endif$

% ── Document ───────────────────────────────────────────────────────────────
\begin{document}

$if(title)$
\maketitle
\thispagestyle{fancy}
$endif$

$if(toc)$
{\hypersetup{linkcolor=headerbg}
\tableofcontents}
\newpage
$endif$

$body$

\end{document}
